默认模型路径: artifacts/scenarios/doom2/ppo_model.zip
说明:
- 这个文件记录的是默认模型保存位置，不是模型本体。
- 第一次训练前，这个位置可能还没有模型文件。
- 运行当前目录下的 `train.py` 后，模型会保存到这里。
- 运行当前目录下的 `play.py` 时，默认也会从这里读取模型。
- 如果你以后改了公共底层脚本中的保存逻辑，这里的路径说明也应该同步修改。
